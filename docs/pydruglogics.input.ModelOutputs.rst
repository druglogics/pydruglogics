pydruglogics.input.ModelOutputs
===============================

Handles model outputs for each node, including loading from file or dictionary,
computing min/max output, and exposing accessors.

.. automethod:: pydruglogics.input.ModelOutputs.ModelOutputs.get_model_output

    Get the output weight for a given node.

.. automethod:: pydruglogics.input.ModelOutputs.ModelOutputs.size

    Return the number of outputs (nodes).


.. automethod:: pydruglogics.input.ModelOutputs.ModelOutputs.print

    Print the string representation of the ModelOutputs instance.

